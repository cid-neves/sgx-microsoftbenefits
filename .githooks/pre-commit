#!/bin/sh
# Auto-update version.json with build timestamp on every commit
BUILT=$(date -u '+%Y-%m-%d %H:%M UTC')
BUILD=$(( $(git rev-list HEAD --count 2>/dev/null || echo 0) + 1 ))
printf '{\n  "built": "%s",\n  "build": %d\n}\n' "$BUILT" "$BUILD" > version.json
git add version.json
