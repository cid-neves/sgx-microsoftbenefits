# Data files — never deploy these, they live on the server
api/data.json
api/snapshots/
api/*.lock

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (if you add build tools later)
node_modules/
EOF